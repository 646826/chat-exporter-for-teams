export const site = {
  origin: 'https://646826.github.io/chat-exporter-for-teams/',
  repo: 'https://github.com/646826/chat-exporter-for-teams',
  name: 'Chat Exporter for Microsoft Teams — Files & ZIP',
};

const sourceButton = '<a class="button" href="https://github.com/646826/chat-exporter-for-teams">View source on GitHub</a>';
const guideCards = `
<div class="guide-list">
  <a class="guide" href="./how-to-export-microsoft-teams-chat-with-attachments/"><small>Step-by-step</small><h3>Export a Teams chat with attachments</h3><p>Capture the open history and keep accessible files in the same archive.</p></a>
  <a class="guide" href="./download-teams-chat-history-without-admin/"><small>No admin API</small><h3>Download Teams chat history without admin access</h3><p>Understand the difference between a local visible-chat export and tenant-wide compliance tools.</p></a>
  <a class="guide" href="./teams-chat-export-html-json-csv-zip/"><small>Choose a format</small><h3>Teams chat export: HTML, JSON, CSV, and ZIP</h3><p>Learn what each file is for and why one archive includes all formats.</p></a>
</div>`;

export const pages = [
  {
    route: '',
    title: 'Export Microsoft Teams Chat with Attachments | Free Chrome Extension',
    description: 'Download Microsoft Teams chat history with messages, images, files and links in one local ZIP. Free, open source and no admin access.',
    structuredData: {
      '@context': 'https://schema.org',
      '@type': 'SoftwareApplication',
      name: site.name,
      applicationCategory: 'BusinessApplication',
      operatingSystem: 'Chrome, Chromium, Microsoft Edge',
      softwareVersion: '0.1.0',
      description: 'Local-first browser extension that exports the open Microsoft Teams chat and accessible attachments to one ZIP.',
      url: site.origin,
      codeRepository: site.repo,
      license: `${site.repo}/blob/main/LICENSE`,
      offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
    },
    body: `
<section class="hero"><div class="wrap hero-grid"><div><span class="eyebrow">Local-first browser extension</span><h1>Export Microsoft Teams chat history with attachments</h1><p class="lead">Save the currently open personal or group conversation, accessible files, links, and diagnostics in one private ZIP — without admin access or a developer cloud.</p><div class="actions">${sourceButton}<a class="button secondary" href="#how-it-works">See how it works</a></div><ul class="trust"><li>Messages + accessible files</li><li>HTML, JSON, and CSV</li><li>No telemetry</li><li>Open source</li></ul></div><img class="hero-shot" src="./assets/screenshot-export.png" width="1280" height="800" alt="Chat Exporter popup beside an open Microsoft Teams conversation and a ZIP file tree"></div></section>
<section class="section"><div class="wrap"><div class="section-head"><h2>One action. One complete archive.</h2><p>The product is intentionally narrow: open a conversation, start the export, and receive readable and structured data together.</p></div><div class="grid3"><article class="card"><div class="iconbox">↥</div><h3>Loads older history</h3><p>Teams virtualizes long chats. The exporter records each visible batch while moving toward the beginning instead of saving only the final screen.</p></article><article class="card"><div class="iconbox">📎</div><h3>Keeps accessible files</h3><p>Images and files available to the signed-in Microsoft 365 session are placed under <code>attachments/</code>. Failures stay visible in the report.</p></article><article class="card"><div class="iconbox">⌂</div><h3>Runs locally</h3><p>No account, analytics, telemetry, or developer backend. The archive is assembled in the active browser tab and downloaded to the device.</p></article></div></div></section>
<section class="section soft"><div class="wrap archive"><div class="filetree"><span class="folder">teams-chat-project-alpha.zip/</span><br>├── <b>chat.html</b> <span class="muted"># offline transcript</span><br>├── <b>chat.json</b> <span class="muted"># structured data</span><br>├── <b>chat.csv</b> <span class="muted"># spreadsheet rows</span><br>├── <b>links.csv</b><br>├── <b>attachments-report.csv</b><br>├── <b>failed-attachments.html</b><br>├── <b>README.txt</b><br>└── <span class="folder">attachments/</span><br>&nbsp;&nbsp;&nbsp;&nbsp;├── proposal.pdf<br>&nbsp;&nbsp;&nbsp;&nbsp;├── screenshot.png<br>&nbsp;&nbsp;&nbsp;&nbsp;└── notes.docx</div><div><span class="eyebrow">Portable by design</span><h2>Readable today. Structured for later.</h2><div class="checks"><div class="checkrow"><span class="checkdot">✓</span><div><strong>Open <code>chat.html</code> offline</strong><br><span class="language">Search the conversation and follow relative links to included assets.</span></div></div><div class="checkrow"><span class="checkdot">✓</span><div><strong>Use JSON in scripts and local tools</strong><br><span class="language">A stable schema keeps message metadata, reactions, links, and attachment records.</span></div></div><div class="checkrow"><span class="checkdot">✓</span><div><strong>Open CSV in a spreadsheet</strong><br><span class="language">Unicode-safe rows make filtering, auditing, or handoff straightforward.</span></div></div><div class="checkrow"><span class="checkdot">✓</span><div><strong>Know what did not download</strong><br><span class="language">Every discovered attachment is reported as downloaded, failed, or skipped.</span></div></div></div></div></div></section>
<section class="section" id="how-it-works"><div class="wrap"><div class="section-head"><h2>How the Teams chat export works</h2><p>No Graph setup, tenant administrator, or format-selection wizard is required for the conversation already visible to you.</p></div><div class="steps"><article class="step"><h3>Open the chat</h3><p>Sign in to Teams on the web and select the personal or group conversation you need.</p></article><article class="step"><h3>Start the export</h3><p>Open Chat Exporter, choose whether to include accessible files, and click the primary button.</p></article><article class="step"><h3>Save the ZIP</h3><p>Keep the tab open while older history and files load. The panel provides progress, cancellation, and the final download.</p></article></div><img class="shot" src="./assets/screenshot-progress.png" width="1280" height="800" alt="Progress panel loading a long Microsoft Teams chat history"></div></section>
<section class="section soft"><div class="wrap privacy-grid"><div><span class="eyebrow">Privacy boundary</span><h2>Temporary access, not permanent surveillance</h2><p class="lead">The extension activates only after your click and requests three narrowly scoped permissions.</p><div class="permission"><code>activeTab</code><span>Temporary access to the current tab after explicit user action.</span></div><div class="permission"><code>scripting</code><span>Injects the bundled local exporter into that active Teams tab.</span></div><div class="permission"><code>storage</code><span>Remembers the disclosure acknowledgement and file preference — not the conversation.</span></div></div><div><div class="notice"><strong>No developer-controlled data endpoint</strong><p>The developer does not receive messages, names, URLs, files, export events, or error logs.</p></div><br><div class="notice warning"><strong>Microsoft 365 rules still apply</strong><p>The exporter cannot bypass permissions, Conditional Access, retention, CORS, expired URLs, or deleted files.</p></div><img class="shot" src="./assets/screenshot-files.png" width="1280" height="800" alt="ZIP attachment folder and file status report" loading="lazy"></div></div></section>
<section class="section"><div class="wrap"><div class="section-head"><h2>Guides for common export questions</h2><p>Detailed instructions explain scope, attachments, formats, and the difference from organization-wide compliance export.</p></div>${guideCards}</div></section>
<section class="section soft"><div class="wrap faq"><div class="section-head"><h2>Frequently asked questions</h2></div><details><summary>Can it export every chat in my organization?</summary><p>No. Version 0.1.0 exports the currently open personal or group conversation. Tenant-wide export belongs to Microsoft Graph, Purview, eDiscovery, or other administrator/compliance workflows.</p></details><details><summary>Does it include attachments?</summary><p>It attempts to include images and files that the current signed-in browser session can retrieve. Every candidate appears in the attachment report even when Microsoft 365 or the browser blocks automatic download.</p></details><details><summary>Is the chat uploaded to a server?</summary><p>No developer server exists for this extension. Capture, rendering, ZIP creation, and download happen locally. The Teams website still communicates with Microsoft services as normal.</p></details><details><summary>Why is a file marked failed?</summary><p>Common reasons include expired permissions, deleted content, a sign-in or viewer page returned instead of the file, Conditional Access, CORS, or explicit size limits.</p></details><details><summary>Does it export channel posts?</summary><p>Not in the first release. The supported promise is the currently open personal or group chat.</p></details></div></section>`,
  },
  {
    route: 'how-to-export-microsoft-teams-chat-with-attachments/',
    title: 'How to Export Microsoft Teams Chat with Attachments',
    description: 'Step-by-step guide to export an open Microsoft Teams chat with messages, images, accessible files, links and attachment diagnostics in one ZIP.',
    body: `<section class="article-hero"><div class="wrap"><span class="eyebrow">Step-by-step guide</span><h1>How to export Microsoft Teams chat with attachments</h1><p class="lead">A visible-chat export can preserve the conversation and files available to your signed-in account without requiring organization-wide administrator access.</p><div class="badge-line"><span class="pill">Personal chats</span><span class="pill">Group chats</span><span class="pill">Accessible files</span><span class="pill">Local ZIP</span></div></div></section><div class="wrap article"><article class="prose"><h2 id="before">Before you begin</h2><p>Open Microsoft Teams in Chrome or a current Chromium-based Edge build and sign in with the account that can see the conversation. Select the conversation itself rather than the Files tab. Wait until messages are visible.</p><div class="notice warning"><strong>Scope matters.</strong> This workflow exports the currently open personal or group chat. It is not a tenant-wide compliance export and does not bypass Microsoft 365 policy.</div><h2 id="steps">Export the conversation and files</h2><ol><li>Open Chat Exporter from the browser toolbar.</li><li>Review the first-run notice explaining that the open conversation and accessible file links are processed locally.</li><li>Leave <strong>Include accessible files</strong> enabled when you need attachments.</li><li>Click <strong>Export chat to ZIP</strong>.</li><li>Keep the Teams tab open. The in-page panel loads older virtualized messages, counts attachment candidates, and downloads files it can access.</li><li>Save the ZIP when the panel reports that the archive is ready.</li></ol><img class="shot" src="../assets/screenshot-progress.png" width="1280" height="800" alt="Chat Exporter loading older Microsoft Teams messages"><h2 id="inside">What is inside the ZIP?</h2><table><thead><tr><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>chat.html</code></td><td>Searchable offline conversation with safe formatting and relative file links.</td></tr><tr><td><code>chat.json</code></td><td>Structured message and attachment data for scripts or local analysis.</td></tr><tr><td><code>chat.csv</code></td><td>One message per row for Excel and spreadsheets.</td></tr><tr><td><code>links.csv</code></td><td>Ordinary links shared in the chat.</td></tr><tr><td><code>attachments/</code></td><td>Images and files the signed-in session could retrieve.</td></tr><tr><td><code>attachments-report.csv</code></td><td>Status, source URL, resolved URL, size, MIME type, and failure reason for each candidate.</td></tr><tr><td><code>failed-attachments.html</code></td><td>Clickable recovery list for files that require manual download.</td></tr></tbody></table><h2 id="failures">Why some Teams attachments cannot be downloaded</h2><p>A message can display a file card even when a direct background-style fetch is not available. Microsoft 365 may require another interactive sign-in, return a SharePoint viewer page, enforce Conditional Access, reject a cross-origin request, or remove access after the message was sent.</p><p>The exporter does not hide these cases. Open <code>attachments-report.csv</code> and use <code>failed-attachments.html</code> while signed in to retrieve the file manually when policy permits.</p><h2 id="verify">Verify a long export</h2><p>Compare the first and last message in <code>chat.html</code> with Teams. The exporter refuses to create a ZIP when its bounded history traversal cannot confirm the beginning, but manual verification remains valuable because Teams can change its interface.</p><div class="cta-band"><div><h2>Keep messages and accessible files together</h2><p>Review the open-source implementation and validated release package.</p></div>${sourceButton}</div></article><aside class="toc"><strong>On this page</strong><a href="#before">Before you begin</a><a href="#steps">Export steps</a><a href="#inside">Archive contents</a><a href="#failures">Attachment failures</a><a href="#verify">Verify the result</a></aside></div>`,
  },
  {
    route: 'download-teams-chat-history-without-admin/',
    title: 'Download Teams Chat History Without Admin Access',
    description: 'Learn how a local browser extension can download the currently open Teams chat without Graph or admin consent, and where that scope ends.',
    body: `<section class="article-hero"><div class="wrap"><span class="eyebrow">Scope explained</span><h1>Download Teams chat history without admin access</h1><p class="lead">You can archive a conversation already rendered for your account without requesting tenant-wide Microsoft Graph or compliance permissions — but only within the boundaries of what the browser can see and access.</p></div></section><div class="wrap article"><article class="prose"><h2 id="meaning">What “without admin access” means</h2><p>Chat Exporter does not ask an administrator to approve a Microsoft Graph application. It runs after your explicit click in the Teams web tab and reads the conversation currently available to that signed-in account.</p><p>This is appropriate for a personal working archive, migration of your own accessible conversation, or local records where organizational policy permits it. It is not a substitute for legal discovery, retention, audit, or organization-wide backup.</p><h2 id="comparison">Visible-chat export vs. administrator export</h2><table><thead><tr><th>Capability</th><th>Local visible-chat export</th><th>Graph / Purview / eDiscovery</th></tr></thead><tbody><tr><td>Setup</td><td>Install extension and open a conversation</td><td>Tenant configuration, roles, consent, or compliance access</td></tr><tr><td>Scope</td><td>Current personal or group chat rendered for the user</td><td>Potentially broader mailbox, tenant, legal, or policy scope</td></tr><tr><td>Attachments</td><td>Attempts files accessible to the current browser session</td><td>Depends on official API and compliance workflow</td></tr><tr><td>Output</td><td>Local ZIP with HTML, JSON, CSV, links, and reports</td><td>API records, cases, PST or other administrator-managed outputs</td></tr><tr><td>Authorization bypass</td><td>None</td><td>None; uses formal administrator/compliance permissions</td></tr></tbody></table><h2 id="permissions">Why the extension still needs browser permissions</h2><p>The three Manifest V3 permissions are narrow and user-triggered:</p><ul><li><code>activeTab</code> gives temporary access after you invoke the extension.</li><li><code>scripting</code> injects local code into that tab so it can inspect the rendered virtualized chat and use the signed-in page context.</li><li><code>storage</code> remembers only the disclosure acknowledgement and file preference.</li></ul><p>There are no static host permissions and no persistent content script that watches every Teams page.</p><h2 id="policy">Respect organizational policy</h2><p>Technical access does not automatically grant organizational permission to archive data. Follow your employer's confidentiality, retention, legal, and acceptable-use rules. Protect the resulting ZIP because it may contain personal data, business records, or confidential attachments.</p><div class="notice warning"><strong>The extension cannot make prohibited data export permissible.</strong> It also cannot bypass retention, Conditional Access, CORS, deleted files, or permissions.</div><h2 id="workflow">Practical workflow</h2><ol><li>Confirm that local archiving is permitted.</li><li>Open the exact Teams personal or group chat.</li><li>Start Chat Exporter and choose whether to include files.</li><li>Keep the tab open until the ZIP is ready.</li><li>Store the archive in an approved location with appropriate access controls.</li><li>Delete temporary copies when your policy requires it.</li></ol><div class="cta-band"><div><h2>Need the exact archive format?</h2><p>See how HTML, JSON, CSV, and file diagnostics work together.</p></div><a class="button" href="../teams-chat-export-html-json-csv-zip/">Compare formats</a></div></article><aside class="toc"><strong>On this page</strong><a href="#meaning">Meaning</a><a href="#comparison">Compare methods</a><a href="#permissions">Permissions</a><a href="#policy">Policy boundary</a><a href="#workflow">Workflow</a></aside></div>`,
  },
  {
    route: 'teams-chat-export-html-json-csv-zip/',
    title: 'Microsoft Teams Chat Export: HTML, JSON, CSV, and ZIP',
    description: 'Compare HTML, JSON and CSV for a Microsoft Teams chat export and see why one ZIP should include readable, structured and diagnostic formats.',
    body: `<section class="article-hero"><div class="wrap"><span class="eyebrow">Format guide</span><h1>Microsoft Teams chat export: HTML, JSON, CSV, and ZIP</h1><p class="lead">No single format serves reading, spreadsheets, automation, and attachment recovery equally well. A portable archive should include all of them.</p></div></section><div class="wrap article"><article class="prose"><h2 id="zip">Why ZIP is the container</h2><p>ZIP keeps the transcript, structured data, diagnostic files, and accessible attachments together with relative paths. Standard archive software can open it without a proprietary account or cloud service.</p><pre>chat.html
chat.json
chat.csv
links.csv
attachments-report.csv
failed-attachments.html
README.txt
attachments/</pre><h2 id="html">HTML: best for reading</h2><p><code>chat.html</code> is the primary human-readable result. It preserves safe rich text, authors, dates, reactions, links, and relative references to downloaded files. Active page content such as scripts, iframes, event handlers, forms, and site-specific styling is removed.</p><p>Use HTML for offline reading, search, review, and handoff to someone who should not need a data tool.</p><h2 id="json">JSON: best for structured processing</h2><p><code>chat.json</code> uses the schema identifier <code>chat-exporter-for-teams/v1</code>. Each message carries its order, ID, author, timestamps, text, sanitized HTML, reactions, links, and attachment candidates. The file also includes public attachment status records.</p><p>Use JSON for local scripts, data transformations, indexing, or importing into tools that can handle structured content. Review privacy and company policy before passing the archive to any AI or cloud service.</p><h2 id="csv">CSV: best for spreadsheets</h2><p><code>chat.csv</code> uses a UTF-8 byte order mark and one message per row. Multiline text, links, reactions, and attachment URLs are quoted correctly. Use it for filtering by author or time, manual audit, and spreadsheet analysis.</p><p>CSV is intentionally flatter than JSON. It cannot preserve nested structures as precisely, so it should not be the only archival format.</p><h2 id="reports">Attachment and link reports</h2><p><code>links.csv</code> separates ordinary links from file candidates. <code>attachments-report.csv</code> records exactly one final state for every discovered candidate:</p><ul><li><strong>downloaded</strong> — included under <code>attachments/</code>;</li><li><strong>failed</strong> — a usable file was not returned;</li><li><strong>skipped</strong> — file downloads were disabled or an explicit size boundary prevented inclusion.</li></ul><p><code>failed-attachments.html</code> makes the original URLs easy to reopen while signed in. This is more honest than silently dropping blocked files.</p><h2 id="choose">Which format should you use?</h2><table><thead><tr><th>Goal</th><th>Best file</th></tr></thead><tbody><tr><td>Read and search the conversation offline</td><td><code>chat.html</code></td></tr><tr><td>Write a script or preserve nested metadata</td><td><code>chat.json</code></td></tr><tr><td>Filter rows in Excel or Sheets</td><td><code>chat.csv</code></td></tr><tr><td>Review shared web links</td><td><code>links.csv</code></td></tr><tr><td>Audit file success and failure</td><td><code>attachments-report.csv</code></td></tr><tr><td>Keep the complete package together</td><td>The ZIP itself</td></tr></tbody></table><div class="cta-band"><div><h2>Export once instead of choosing upfront</h2><p>Chat Exporter builds every useful format in the same local archive.</p></div>${sourceButton}</div></article><aside class="toc"><strong>On this page</strong><a href="#zip">ZIP container</a><a href="#html">HTML</a><a href="#json">JSON</a><a href="#csv">CSV</a><a href="#reports">Reports</a><a href="#choose">Choose a format</a></aside></div>`,
  },
  {
    route: 'privacy/',
    title: 'Privacy Policy | Chat Exporter for Microsoft Teams',
    description: 'Privacy policy for Chat Exporter: local processing, no developer server, no telemetry, minimal browser permissions and Microsoft 365 boundaries.',
    body: `<section class="article-hero"><div class="wrap"><span class="eyebrow">Effective August 2, 2026</span><h1>Privacy policy</h1><p class="lead">Chat Exporter processes the conversation requested by the user in the browser and does not send it to the developer.</p></div></section><div class="wrap article"><article class="prose"><h2 id="summary">Summary</h2><p>Chat Exporter is a local-first extension. It reads the Microsoft Teams conversation the user explicitly opened, optionally requests file URLs available to the same signed-in browser session, and creates a ZIP on the user's device.</p><p>The developer does not operate a server for the extension and does not receive conversation text, attachments, URLs, filenames, names, export metadata, usage events, or error logs.</p><h2 id="data">Data processed</h2><ul><li>message text and sanitized formatting;</li><li>author names, timestamps, message identifiers, reactions, and links rendered in the open conversation;</li><li>attachment names, URLs, types, sizes, and file contents accessible to the signed-in user;</li><li>the current Teams page URL and visible conversation title;</li><li>the disclosure acknowledgement and include-files preference.</li></ul><h2 id="use">Purpose</h2><p>Data is used only to traverse the open chat, retrieve selected accessible files, render local output, assemble the archive, and show progress or errors. It is not used for advertising, profiling, analytics, machine-learning training, or unrelated purposes.</p><h2 id="transmission">Transmission</h2><p>No conversation data is transmitted to a developer-controlled endpoint. Microsoft Teams and Microsoft 365 continue to communicate with Microsoft services as part of the website. Optional file requests go from the active page to Microsoft-hosted URLs available to the signed-in user.</p><h2 id="storage">Storage and retention</h2><p>Only the acknowledgement and file preference are stored in extension storage. Conversation data is held in browser memory for the export. The resulting ZIP is saved where the browser directs it; the extension keeps no separate copy.</p><h2 id="permissions">Permissions</h2><ul><li><code>activeTab</code> — temporary access after explicit action;</li><li><code>scripting</code> — inject local export code into that Teams tab;</li><li><code>storage</code> — save the two non-sensitive preferences.</li></ul><p>No broad host, cookie, identity, history, webRequest, or native-messaging permission is requested.</p><h2 id="security">Security</h2><p>The project uses Manifest V3, local executable code, safe DOM construction, exported-HTML sanitization, field escaping, ZIP path validation, deterministic packaging, and an explicit package allowlist. Users should protect exported ZIP files because they may contain confidential data.</p><h2 id="limits">Microsoft 365 limitations</h2><p>The extension cannot bypass permissions, retention, Conditional Access, cross-origin restrictions, expired links, deleted files, or tenant policy. Unavailable files are recorded in local diagnostics.</p><h2 id="changes">Changes and contact</h2><p>Material changes are recorded in repository history and reflected by a new effective date. Questions may be sent to <code>646826@gmail.com</code>.</p><p>Microsoft and Microsoft Teams are trademarks of the Microsoft group of companies. This independent extension is not affiliated with, endorsed by, or sponsored by Microsoft.</p></article><aside class="toc"><strong>On this page</strong><a href="#summary">Summary</a><a href="#data">Data processed</a><a href="#use">Purpose</a><a href="#transmission">Transmission</a><a href="#storage">Storage</a><a href="#permissions">Permissions</a><a href="#security">Security</a><a href="#limits">Limits</a><a href="#changes">Contact</a></aside></div>`,
  },
  {
    route: 'support/',
    title: 'Support | Chat Exporter for Microsoft Teams',
    description: 'Troubleshoot Teams chat detection, long-history export, missing attachments, ZIP downloads, cancellation and safe issue reporting.',
    body: `<section class="article-hero"><div class="wrap"><span class="eyebrow">Troubleshooting</span><h1>Chat Exporter support</h1><p class="lead">Resolve chat detection, long-history, attachment, and ZIP download problems without sharing private conversation data.</p></div></section><div class="wrap article"><article class="prose"><h2 id="prepare">Before exporting</h2><ol><li>Use a current Chrome or Chromium-based Edge build.</li><li>Sign in to Teams on the web.</li><li>Open the conversation itself, not the Files tab.</li><li>Wait for visible messages to load.</li><li>Keep the tab open and do not switch chats during export.</li></ol><h2 id="detect">The extension cannot detect the chat</h2><p>It runs only on <code>teams.cloud.microsoft</code> and <code>teams.microsoft.com</code>. Reload the tab, reopen the personal or group conversation, wait for messages, and try again. Channel posts are outside version 0.1.0 scope.</p><h2 id="long">A long history stops</h2><p>The exporter uses a bounded completeness check and refuses to make a partial ZIP when it cannot confirm the beginning. Retry with the tab in the foreground. Report repeatable failures with the extension version, browser version, domain, approximate message count, and non-sensitive test steps.</p><h2 id="files">Some files are missing</h2><p>Open <code>attachments-report.csv</code> and <code>failed-attachments.html</code>. Permission loss, deletion, expired URLs, viewer/sign-in HTML, Conditional Access, CORS, and size limits can prevent inclusion. Use the original link while signed in when policy allows manual download.</p><h2 id="download">The ZIP did not download</h2><p>Use the persistent <strong>Download ZIP</strong> button in the progress panel. Check browser download settings, hidden save dialogs, disk space, and endpoint security controls. Do not reload the tab before saving.</p><h2 id="cancel">Cancellation</h2><p>Cancellation stops capture and file requests. No partial ZIP is produced. Start a new export to retry.</p><h2 id="issue">Report an issue safely</h2><p>Open a GitHub issue with the extension version, browser and OS, Teams domain, chat type, approximate counts, exact error, and non-sensitive reproduction. Never attach a real archive, signed file URL, token, email address, or confidential screenshot.</p><p>Send security or privacy reports privately to <code>646826@gmail.com</code>.</p><div class="cta-band"><div><h2>Inspect the implementation</h2><p>Tests, selectors, permissions, and release validation are public.</p></div>${sourceButton}</div></article><aside class="toc"><strong>On this page</strong><a href="#prepare">Prepare</a><a href="#detect">Chat detection</a><a href="#long">Long history</a><a href="#files">Missing files</a><a href="#download">ZIP download</a><a href="#cancel">Cancellation</a><a href="#issue">Report an issue</a></aside></div>`,
  },
];
